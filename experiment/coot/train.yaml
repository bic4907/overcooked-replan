program: baselines/CooT/train_overcooked_v3.py
name: coot-role-scenarios-reindexed-v6
method: grid
metric:
  goal: minimize
  name: validation/loss

parameters:
  scenario:
    values:
      - split_0
      - split_1
      - outage_0
      - outage_1
      - recipe_switch_0
      - recipe_switch_1
      - distance_switch_0
      - distance_switch_1
  SEED:
    # Match the six training seeds used by the existing FCP/Self-play sweeps.
    values: [0, 1, 2, 3, 4, 5]
  # Batch 16 measured about 12 GB; 32 leaves peak headroom on a 40 GB GPU.
  BATCH_SIZE:
    value: 32
  # All 21 pair shards fit comfortably in the training hosts' RAM and avoiding
  # repeated NPZ decompression keeps the accelerator input pipeline fed.
  SHARD_CACHE_SIZE:
    value: 21
  PREFETCH_BATCHES:
    value: true
  wandb_mode:
    value: online
  ENTITY:
    value: cilab-overcooked
  PROJECT:
    value: overcooked-v3-coot-train

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens}
