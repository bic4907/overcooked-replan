program: baselines/CooT/pipeline_stage_overcooked_v3.py
name: coot-build-training-datasets
method: grid
metric:
  goal: maximize
  name: pipeline/completed

parameters:
  stage:
    value: build_dataset
  layout:
    values: [split_0, split_1, split_2, outage_0, outage_1, outage_2, recipe_switch_0, recipe_switch_1, recipe_switch_2, distance_switch_0, distance_switch_1, distance_switch_2]
  # Must match score_and_select.yaml so pair construction resolves the same 21
  # HSP-only proxy members.
  allow-low-return-fill:
    value: true
  entity:
    value: cilab-overcooked
  project:
    value: overcooked-v3-coot-build-dataset

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_equals}
