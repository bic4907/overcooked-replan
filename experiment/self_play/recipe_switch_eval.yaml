program: baselines/IPPO/eval_crossplay_overcooked_v3.py
name: recipe-switch-seedwise-xp
method: grid
metric:
  goal: maximize
  name: XP

parameters:
  algorithms:
    value: IPPO
  layout:
    values:
      - recipe_switch_0
      - recipe_switch_1
      - recipe_switch_2
  episodes:
    value: 20
  max-steps:
    value: 450
  workers-per-gpu:
    value: 8

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - inchangbaek4907/overcooked-v3-role-coordination
  - ${args_no_equals}
