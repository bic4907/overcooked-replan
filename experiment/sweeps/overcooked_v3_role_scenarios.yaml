name: overcooked-v3-role-scenarios-v4
program: baselines/IPPO/ippo_overcooked_v3.py
project: overcooked-v3-role-coordination
method: grid
run_cap: 20

metric:
  goal: maximize
  name: train/episode_return

parameters:
  scenario:
    values:
      - split_no_sig
      - split_sig
      - outage_no_sig
      - outage_sig
  SEED:
    values: [0, 1, 2, 3, 4]
  NUM_SEEDS:
    value: 1
  ARCHITECTURE:
    value: cnn
  PROJECT:
    value: overcooked-v3-role-coordination
  WANDB_MODE:
    value: online
  SAVES_DIR:
    value: saves
  EXPERIMENT_FOLDER:
    value: role-scenarios-v4
  recording:
    value: enabled

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens}
