program: baselines/IPPO/ippo_overcooked_v3.py
method: grid
metric:
  goal: maximize
  name: train/episode_return

parameters:
  scenario:
    values:
      - splitnosig_0
      - splitsig_0
      - outagenosig_0
      - outagesig_0
  SEED:
    values: [0, 1, 2, 3, 4, 5]
  recording:
    value: enabled

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens}
