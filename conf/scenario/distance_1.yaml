# @package _global_

ENV_KWARGS:
  layout: distance_1
  max_steps: 450
  random_agent_positions: false
RECORD_MAX_STEPS: 450

EXPERIMENT: distance
CONDITION: distance_1
WANDB_GROUP: distance
LAYOUT_REVISION: distance-wide-v2
