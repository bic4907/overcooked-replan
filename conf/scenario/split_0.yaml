# @package _global_

ENV_KWARGS:
  layout: split_0
  max_steps: 450
  random_agent_positions: false
RECORD_MAX_STEPS: 450

EXPERIMENT: kitchen_split
CONDITION: split_0
WANDB_GROUP: kitchen_split
LAYOUT_REVISION: split-narrow-diagonal-v1
