# @package _global_

ENV_KWARGS:
  layout: distance_0
  max_steps: 450
  random_agent_positions: false
RECORD_MAX_STEPS: 450

EXPERIMENT: distance
CONDITION: distance_0
WANDB_GROUP: distance
LAYOUT_REVISION: distance-inversion-shifted-stations-13x6-v2
