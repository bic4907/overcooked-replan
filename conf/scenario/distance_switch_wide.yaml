# @package _global_

ENV_KWARGS:
  layout: distance_switch_wide
  max_steps: 450
  random_agent_positions: false
RECORD_MAX_STEPS: 450

EXPERIMENT: distance_switch
CONDITION: distance_switch_wide
WANDB_GROUP: distance_switch
LAYOUT_REVISION: distance-switch-wide-v2
